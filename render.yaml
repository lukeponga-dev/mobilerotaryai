services:
  - type: web
    name: ai-mechanic
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    pullRequestPreviewsEnabled: true
    envVars:
      - key: API_KEY
        sync: false